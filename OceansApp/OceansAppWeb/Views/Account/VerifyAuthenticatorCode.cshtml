@using OceansApp.Models.ViewModels
@model VerifyAuthenticatorVM

<partial name="_LoginStylesPartial" />

<div class="text-center">
    <div class="custom-box">
        <div class="sub-box">
           <div>
                <h2 class="text-center">Verificación Código de Autenticación</h2>
            <form asp-controller="Account" asp-action="VerifyAuthenticatorCode" asp-route-returnurl="@ViewData["ReturnUrl"]" method="post" role="form">
                <p>Por favor ingresa el código que se desplega en tu aplicación móvil de autenticación.</p>
                <input asp-for="RememberMe" type="hidden" />
                <hr />
                <div class="form-group">
                    <div class="">
                        <input placeholder="Código" asp-for="Code" class="form-control" />
                        <span asp-validation-for="Code" class="text-danger"></span>
                    </div>
                </div>
                <div class="form-group text-left">
                    <br />
                    <div class="">
                        <button type="submit" class="btn btn-primary">Enviar</button>
                    </div>
                </div>
            </form>
           </div>
        </div>
    </div>
</div>

@section Scripts{
    @{
    await Html.RenderPartialAsync("_ValidationScriptsPartial");
 }
}